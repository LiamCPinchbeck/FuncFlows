# Theory

Three pages. If you only read one, read the first — it's the one that explains what object
we're actually pushing around, and why it's a measure and not a density. The other two are
"why the traces matter" and "how the sampler works", and both assume the first.

```{toctree}
:maxdepth: 1

function_space_flows
traces
latent_pcn
```
